const a = 5
const b = 7

const max = a < b ? b : a
console.log(max) // 7

let value = 11
console.log(value >=0 ? value : -value) // 11

let value2 = -3
console.log(value2 >=0 ? value2 : -value2) // 11 